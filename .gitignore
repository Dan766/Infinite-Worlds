node_modules/
dist/
.vite/
.firebase/
*.log
.DS_Store

# Output of `npm run shots:check`; the committed baselines in shots/*.png stay.
shots/.check/
# Output of `npm run shots:repeat`, kept only when a view came back unstable.
shots/.repeat/
